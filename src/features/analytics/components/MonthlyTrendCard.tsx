import { Card, CardHeader, CardContent } from '@/components/ui/card';

/**
 * Placeholder card for the monthly trend visualization.
 * Developers can replace the placeholder with a real chart implementation.
 */
export const MonthlyTrendCard = () => (
  <Card>
    <CardHeader>Monthly Trend</CardHeader>
    <CardContent>
      {/* Insert a line chart library, e.g., recharts LineChart */}
      {/* Placeholder – developers can replace with actual chart */}
    </CardContent>
  </Card>
);
